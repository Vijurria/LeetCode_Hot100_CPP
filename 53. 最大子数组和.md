class Solution {
public:
    int maxSubArray(vector<int>& nums) {
        int last=0,maxn=nums[0];
        for(int i=0;i<nums.size();i++)
        {
            last=max(last+nums[i],nums[i]);
            maxn=max(maxn,last);
        }
        return maxn;
    }
};