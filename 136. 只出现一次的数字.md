class Solution {
public:
    int singleNumber(vector<int>& nums) {
        int ans;
        for(int i=0;i<nums.size();i++)
        {
            if(i==0) ans=nums[i];
            else ans^=nums[i];
        }
        return ans;
    }
};